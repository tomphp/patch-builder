language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - composer self-update
  - composer install --prefer-source
 
script:
  - composer test
#  - vendor/bin/phpmd src text codesize

matrix:
    fast_finish: true
    allow_failures:
        - php: hhvm
        - php: 7
